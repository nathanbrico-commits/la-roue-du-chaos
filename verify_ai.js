const dotenv = require('dotenv'); dotenv.config(); const fetch = require('node-fetch'); async function test() { const key = process.env.GEMINI_API_KEY; const url = 'https://generativelanguage.googleapis.com/v1beta/models/gemini-flash-latest:generateContent?key=' + key; const resp = await fetch(url, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ contents: [{ parts: [{ text: 'Génère un défi court pour une soirée.' }] }] }) }); const data = await resp.json(); console.log(JSON.stringify(data, null, 2)); } test();
